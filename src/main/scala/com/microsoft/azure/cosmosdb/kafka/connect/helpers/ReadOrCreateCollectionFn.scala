package com.microsoft.azure.cosmosdb.kafka.connect.helpers

object ReadCollection{
    // def readOrCreateCollection(database: Database, settings: CosmosDBSinkSettings)(implicit documentClient: DocumentClient) : DocumentCollection = {
    //     // logger.info(s"Checking ${settings.collection} exists")

    //     // logger.info(s"Could not find ${settings.collection}")

    //     // logger.info(s"Creating ${settings.collection} collection")

    //     // logger.info(s"Collection ${settings.collection} created")        
    // }
}
